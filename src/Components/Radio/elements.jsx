import styled from "styled-components";
import { Radio } from "antd";

export const StyledRadioGroup = styled((props) => <Radio.Group {...props} />)``;

export const StyledRadioButton = styled((props) => <Radio.Button {...props} />)``;
